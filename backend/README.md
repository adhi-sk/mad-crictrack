# Python scoring engine

The browser/PWA is the production mobile application for GitHub Pages. These Python
modules are retained as a reusable/reference scoring engine and are covered by tests.

They are not executed by the static GitHub Pages app.
